---
"@robino/md": minor
---

Add `?frontmatter` transform to the `md` Vite plugin that validates and exports only Markdown frontmatter, allowing eager metadata imports without pulling lazy Markdown content into the entry chunk. The query requires a configured `FrontmatterSchema`.
