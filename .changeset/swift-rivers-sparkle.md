---
"@robino/tsconfig": minor
---

Use `check.json` as your standard `tsconfig.json`.

For builds, add a `tsconfig.build.json` that extends either `@robino/tsconfig/tsc.json` or `@robino/tsconfig/bundler.json`.
