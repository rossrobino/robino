---
"@robino/md": major
---

Rename `Processor#getHeadings()` to `Processor#headings()` and `Processor#getFrontmatter()` to `Processor#frontmatter()`.

This is a breaking change for callers using those public instance methods directly.
